language: python
python:
  - 3.6

# command to install dependencies
install:
  - pip install --upgrade pip setuptools wheel
  - pip install --only-binary=scipy scipy
  - pip install coveralls
  - pip install codecov

# command to run tests
script:
  - coverage3 run --source=../ tests/

# Add coverall support
after_success:
  - coveralls
  - codecov
